# Database migrations

- Give every Drizzle migration a semantic lowercase snake_case name.
- Generate migrations with `pnpm exec drizzle-kit generate --name <semantic_name>`.
- Do not accept Drizzle's generated word-pair name.
